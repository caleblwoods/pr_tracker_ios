# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PRTracker' do
  use_frameworks!
  pod 'RxSwift',    '~> 4.0'
  pod 'RxCocoa',    '~> 4.0'
  pod 'Firebase/Core'
end
  # Pods for PRTracker

  target 'PRTrackerTests' do
    inherit! :search_paths
    # Pods for testing
  end
